

// perfect number range in c program

#include<stdio.h>

int main()
{
    int min,max;
    int temp,sum = 0;
    printf("Enter the Minimum value: ");
    scanf("%d",&min);
    printf("Enter the max value: ");
    scanf("%d",&max);
    printf("the perfect Number are:\n");

    for(int i = min; i < max; i++)
    {
        for (i = 1; i < max; i++)
        {
            // if()
        }
        
    }
}